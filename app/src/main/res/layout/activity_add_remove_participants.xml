<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ch.epfl.sweng.opengm.events.AddRemoveParticipantsActivity">

    <SearchView
        android:id="@+id/searchMember"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/bluegreen"
        android:layout_alignParentTop="true">
    </SearchView>

    <ListView
        android:id="@+id/memberListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/okayButton"
        android:layout_below="@+id/searchMember">
    </ListView>

    <Button
        android:background="@drawable/rounded_buttons"
        android:textColor="@color/white"
        android:id="@+id/okayButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="clickOnOkayButton"
        android:text="@string/okayButtonText" />
</RelativeLayout>
